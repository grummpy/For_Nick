# Create an API key at https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Optional: a Vector Store ID created during setup. Uploaded files can be searched with it.
OPENAI_VECTOR_STORE_ID=
# Optional: choose the model for Responses API requests.
OPENAI_MODEL=gpt-5-mini
PORT=3000
